import React from "react";
import { Band } from "taste-of-health-ds";

/** The beige slogan band that follows the hero. */
export const Slogan = () => <Band title="Прості, доступні та здорові рецепти" />;

/** English version. */
export const English = () => <Band title="Easy, Affordable & Healthy Recipes" />;
